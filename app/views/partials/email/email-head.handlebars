<head>
  <script type="application/ld+json">
    {
      "@context": "http://schema.org",
      "@type": "EmailMessage",
      "description": "{{actionDesc}}",
      "potentialAction": {
        "@type": "{{actionType}}",
        "name": "{{actionLabel}}",
        "handler": {
          "@type": "HttpActionHandler",
          "url": "{{cta}}"
        }
      }
    }
  </script>
</head>