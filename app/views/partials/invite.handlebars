{{#if user}}
  <div class="invite">

    <label for="invite-field" class="invite-label">Invite Your Friends</label>
    <input type="text" class="invite-field" placeholder="Email" id="invite-field" name="invites" />

    <label for="message-field" class="message-label">Message</label>
    <textarea placeholder="Enter a message" id="message-field" name="message"></textarea>

  </div>
{{/if}}